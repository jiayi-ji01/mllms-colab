run:
  tokenizer: artifacts/wikipedia_tokenizer/tokenizer.model
  data: data/sva/controlled_v2/test.jsonl
  output_dir: outputs/interpretability/wikipedia_cross_language_v2_trajectory
  device: auto
  language: both
  directions:
    - original-to-original
    - clone-to-clone
    - original-to-clone
    - clone-to-original
  components:
    - head_out
  prediction_position_only: true
  selection: all
  controls:
    - clean
    - opposite-number
    - same-number-shuffled
    - opposite-number-shuffled
  # The same fixed, derangeable cohort as the best-checkpoint confirmatory run.
  max_examples: 1280
  max_input_pairs: null
  example_batch_size: 8
  intervention_batch_size: 64
  max_positions: null
  sanity_margin: 0.0
  denominator_epsilon: 0.000001
